import { SearchAKeywordInComplete } from './search-a-keyword-in-complete';

describe('SearchAKeywordInComplete', () => {
  it('should create an instance', () => {
    expect(new SearchAKeywordInComplete()).toBeTruthy();
  });
});
